{
  "name": "kb-deploy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Atomically deploy a new version of the kb_dataset_uy_v3_deep data into Supabase Postgres.",
  "scripts": {
    "migrate": "node migrate.mjs",
    "deploy": "node deploy.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "pg": "^8.12.0"
  }
}
