project_id = "bzqpqncoeilhzukohynz"

# The kb function uses standard Supabase auth: the gateway verifies the JWT
# (anon key / user access token) and the function additionally requires a real
# signed-in user for every route except GET /. See supabase/functions/kb/index.ts.
[functions.kb]
verify_jwt = true
