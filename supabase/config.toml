project_id = "bzqpqncoeilhzukohynz"

# The kb function does its own authorization (per-app token or end-user JWT), so
# the gateway must NOT pre-require a Supabase JWT. This lets an external app
# authenticate with only its per-app token. See supabase/functions/kb/index.ts.
[functions.kb]
verify_jwt = false
